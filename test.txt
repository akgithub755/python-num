/* ===== Top nav shell ===== */
.top-nav {
    background: #ffffff;
    border-bottom: 1px solid #e5e7eb;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.04);
}

.top-menu {
    display: flex;
    align-items: center;
    gap: 4px;
}

.top-menu-item {
    position: relative;
}

/* ===== Top-level menu trigger (Processes / Projection / Table Management) ===== */
.top-menu-link {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    border-radius: 6px;
    color: #374151;
    font-size: 14px;
    font-weight: 500;
    text-decoration: none;
    transition: background-color 0.15s ease, color 0.15s ease;
}

.top-menu-link:hover {
    background-color: #f3f4f6;
    color: #111827;
}

.top-menu-link.active,
.top-menu-item:has(.top-dropdown.open) .top-menu-link {
    background-color: #f0fdfa;
    color: #0d7377;
}

/* Chevron: rotates smoothly, no jump */
.menu-chevron {
    font-size: 10px;
    color: #9ca3af;
    transition: transform 0.2s ease;
}

.top-menu-item:has(.top-dropdown.open) .menu-chevron {
    transform: rotate(180deg);
    color: #0d7377;
}

/* ===== Dropdown panel ===== */
.top-dropdown {
    position: absolute;
    top: calc(100% + 6px);
    left: 0;
    min-width: 260px;
    background: #ffffff;
    border: 1px solid #e5e7eb;
    border-radius: 10px;
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.08), 0 2px 6px rgba(0, 0, 0, 0.04);
    padding: 6px;
    z-index: 50;

    opacity: 0;
    transform: translateY(-4px);
    pointer-events: none;
    transition: opacity 0.15s ease, transform 0.15s ease;
}

.top-dropdown.open {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
}

.dropdown-header {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 10px 6px;
    font-size: 12px;
    font-weight: 600;
    color: #6b7280;
    text-transform: uppercase;
    letter-spacing: 0.04em;
}

.dropdown-header i {
    font-size: 12px;
    color: #9ca3af;
}

.dropdown-divider {
    height: 1px;
    background: #f0f0f0;
    margin: 4px 6px;
}

/* ===== Individual dropdown links ===== */
.dropdown-link {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 10px;
    border-radius: 6px;
    color: #374151;
    font-size: 13.5px;
    text-decoration: none;
    transition: background-color 0.12s ease;
}

.dropdown-link:hover {
    background-color: #f3f4f6;
}

.dropdown-link.active {
    background-color: #f0fdfa;
    color: #0d7377;
    font-weight: 500;
}

.dropdown-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 26px;
    height: 26px;
    border-radius: 6px;
    background: #f3f4f6;
    color: #6b7280;
    font-size: 12px;
    flex-shrink: 0;
}

.dropdown-link:hover .dropdown-icon {
    background: #e5e7eb;
}

/* ===== Program tree list (nested items) ===== */
.program-list {
    max-height: 320px;
    overflow-y: auto;
    padding: 2px 0;
}

.program-list::-webkit-scrollbar {
    width: 6px;
}

.program-list::-webkit-scrollbar-thumb {
    background: #d1d5db;
    border-radius: 3px;
}