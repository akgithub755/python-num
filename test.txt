/* ===== Chevron refinement ===== */
.menu-chevron {
    font-size: 9px;
    color: #94a3b8;
    margin-left: 2px;
    transition: transform 0.2s ease, color 0.2s ease;
    display: inline-block;
}

.top-menu-item:hover .menu-chevron {
    transform: rotate(180deg);
    color: #03acba;
}

/* Give the chevron a touch more separation from the label so it doesn't
   look cramped against the text */
.top-menu-link {
    letter-spacing: 0.01em;
}

/* ===== Fix the ugly native scrollbar inside the mega menu ===== */
.processes-dropdown,
.program-list {
    scrollbar-width: thin;                 /* Firefox */
    scrollbar-color: #cbd5e1 transparent;  /* Firefox */
    padding-right: 4px;                    /* keep text clear of scrollbar */
}

.processes-dropdown::-webkit-scrollbar,
.program-list::-webkit-scrollbar {
    width: 6px;
}

.processes-dropdown::-webkit-scrollbar-track,
.program-list::-webkit-scrollbar-track {
    background: transparent;
}

.processes-dropdown::-webkit-scrollbar-thumb {
    background-color: #cbd5e1;
    border-radius: 8px;
}

.processes-dropdown::-webkit-scrollbar-thumb:hover {
    background-color: #94a3b8;
}

/* ===== Softer, more "mega menu" feel overall ===== */
.top-dropdown {
    border-radius: 12px;
    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.10), 0 2px 8px rgba(0, 0, 0, 0.04);
}

.processes-dropdown {
    padding: 10px 6px 10px 10px;
}